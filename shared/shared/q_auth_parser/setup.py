# shared/q_auth_parser/setup.py
from setuptools import setup, find_packages

setup(
    name="q_auth_parser",
    version="0.1.0",
    packages=find_packages(),
    description="A shared library for parsing authentication tokens in the Q platform.",
    author="Q Platform Development Team",
    author_email="dev@q-platform.ai",
) 