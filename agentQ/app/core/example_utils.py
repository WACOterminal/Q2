
def add_numbers(a: int, b: int) -> int:
    """
    This is a simple function to add two numbers.
    It is intentionally basic to serve as a target for agent-based refactoring.
    """
    # This comment is a good candidate for removal by a refactoring agent.
    return a + b 